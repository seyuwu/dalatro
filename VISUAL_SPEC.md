# dotora — Visual Spec v4 («decision» edition)

**Направление:** React-прототип (`test/`) как эталон. Оливково-зелёная
«военная» палитра линии, Manrope (текст) + Oswald (цифры/логотип), панели-
карточки с тонкими рамками. Тёмный тёплый фон `#101512`, мята `#a2c7a3` —
прогресс, золото `#ddb17a` — экономика/акценты, синий `#86afc7` — ТП.

**Закон композиции v4:** *центр — место принятия решения; слева — состояние
забега; справа — контекст, помогающий решить; внизу во всю ширину — рука/билд.*

**Экран не скроллится.** Игра живёт в фиксированном app-shell (`#app` =
100vh): топбар → колонки → полоса руки, всё видно одновременно. Рост
контента (связки, альтернативы, журнал) гасится внутренней прокруткой
колонок (sidebar/context/лавка/сцена на узких экранах), а не высотой
страницы. Футер удалён — прогресс акта живёт в сайдбаре, версия в шапке.
Свободная ширина больших мониторов отдаётся колонкам (shell до 1900px).

## Каркас экрана

```
topbar (логотип · навигация Играть/Коллекция/Как играть · мета забега:
        ядро скоринга, акт, seed, автосейв, «Новый забег», debug · звук/настройки)
game-layout: [sidebar ~244px | battle-scene | context ~292px]  (context скрыт на развилке/финалах)
bottom-band (волна: шапка «рука + БИЛД-иконки + сортировка» и 7 карт; лавка: полный билд; развилка: скрыта)
``` (футера нет)

- **Sidebar (HUD забега):** панель «ЗАБЕГ» (акт · волна N/5, шаги акта,
  золото, казармы, пипсы тимфайтов/сбросов — бои только в фазе волны),
  «ЭФФЕКТЫ» (импульс серии; пустое состояние честно пишет «серия не начата»),
  сворачиваемый «ЖУРНАЛ» (кнопка 💡 = совет дня), кнопка справочника.
  В лавке/развилке бейдж и шаги акта показывают *следующую* волну.
- **Battle-scene (центр, только фаза волны):** одна сцена на арте линии
  (без внутренних панелей, лозунги удалены). Сверху цель: эмблема, имя,
  **HP крупным Oswald**, полоса, награда; строкой ниже — **чипы правил
  волны** 11px (Броня/Глиф/Мины/Aegis/проклятия/защита башни; бой,
  заблокированный глифом, — пульсирующий красный чип). Ниже сцена: слева
  формация (слоты 52×62 с именами и тултипами) + кнопка-комбо (открывает
  расчёт) + **формула — главное число экрана** (блоки 78px, урон 52px
  Oswald); справа колонка: **приговор** → чипы ставки/импульса →
  «В бой»/«сброс».
- **Приговор (verdict):** только для неочевидных исходов — обычное
  «сколько останется» показывает красный сегмент полосы, текста нет:
  `БАШНЯ ПАДАЕТ` (мята, урон ≥ HP) · `ЕЩЁ 1 УДАР` (янтарный; только если
  остаток HP ≤ лучшего хараса из руки — порог гоняет Sim по одиночным картам,
  без субъективных состояний) · `ГЛИФ: УРОН = 0` (красный).
  Пустой выбор — dashed «ПРИГОВОР» с подсказкой.

- **Перетаскивание формации:** героя в слоте можно зажать и тащить —
  занятые слоты меняются местами, пустой принимает героя; урон, формация,
  связки и альтернативы пересчитываются сразу (pointer-события, порог 6px,
  подсветка целевого слота; запрещено во время анимации боя и исхода).
- **Context (правая колонка):** волна: «ТВОЯ ФОРМАЦИЯ/КОМБИНАЦИЯ» (имя,
  подзаголовок, сила × множ = урон, активные связки) + для формаций
  «ПРОТИВ ЭТОЙ ЦЕЛИ» (альтернативы по урону против защиты этой башни; это
  базы без способностей героев и ставки — указано в легенде) + кнопка
  «Все формации ›» (каталог живёт в справочнике, не висит постоянно).
  Классика: активное комбо + каталог с подсветкой собрано/готово.
  Лавка: «СЛЕДУЮЩАЯ ЦЕЛЬ» (имя, HP, правила волны). Развилка и финалы:
  колонки нет (сетка из двух).
- **Bottom-band:** одна шапка строки: «Твоя рука N/7» → БИЛД-иконки 46×32
  с pop-тултипами (клик = разбор/продажа) → сортировка/колода; ниже рука
  во всю ширину (карты до ~1340px суммарно, арт 100px). Лавка: вместо руки —
  полные карточки билда. Развилка: полоса скрыта.

## Онбординг

- **Интерактивный сценарий** (`src/ui/tutorial.js`, флаг `dotora_tut_v1`),
  принцип — одна строка на подсказку и объяснение в момент действия:
  титул: спотлайт стартеров «выбери билд» → единственная блокирующая
  реплика «вот твой враг» (затемнение, клик в любом месте продолжает) →
  строка над рукой → после первого выбора спотлайт формулы «цифра = сила»
  → звезда-сценарий (Dawnbreaker, если в руке: пульс «возьми», drag на
  3-й слот) → карточка способности появляется только когда её эффект ВИДЕН
  в превью боя («+N к множителю — это она») → пульс на «В бой» → после
  боя: «башня помнит урон» и «сброс» (автозакрытие с hover-паузой) →
  финал-тост. Счётчиков шагов и кнопок «Дальше» нет; «не сейчас» откладывает
  контекстный шаг. Слой живёт вне `#app`, Esc/× — пропуск навсегда.
- **Подсказки первого раза** (после сценария, помечаются показом): первая
  лавка, первая развилка, заминированная волна, «3 одного цвета — до флеша
  один шаг». Формат — пилюля сверху, исчезает сама за ~5с.
- **Модалка «5 шагов»** — справочная: кнопка «Как играть — 5 шагов» на
  титуле и «Как играть» в навигации (там же таблица комбинаций).

## Анатомия карты героя

Портрет с CDN Valve поверх SVG-сигила (автофолбэк, см. IP-заметку) →
виньетка → ранг (Oswald, слева сверху) → символ атрибута (справа) → имя.
Низ карты: атрибут цветом, название способности, ранг ⚡ и хоткей 1–7.
Выбор: подъём −9px, золотая рамка, бейдж порядка выбора. Мина Techies:
затемнение + круглый бейдж 💣 с красной каймой, тултип называет контру.

## Ставка, импульс, мины в UI

- Чипы ставки/импульса в панели хода (`СТАВКА ×1.25`, `ИМПУЛЬС ×1.15`).
- Правила волны строками в сайдбаре; обезвреженные мины — зачёркнуты;
  импульс — тёплым оранжевым с 🔥.
- Разобранный расчёт — модалка «РАСЧЁТ ТИМФАЙТА» (весь стек шаг за шагом),
  открывается кликом по названию комбинации.

## Модалки и мелочи

Справочник (шаги + таблица комбинаций), коллекция (герои/предметы/колода +
поиск), настройки (звук/анимации + клавиши), журнал, подтверждение нового
забега с seed-полем, разбор предмета (советчик «что даст твоему забегу» +
продажа в лавке). Тосты вместо предупреждающих модалок. Горячие клавиши:
`1–7` выбор, `Enter` бой, `R` сброс, `Esc` снять/закрыть, `D` песочница;
лавка: `1–5` купить, `R` реролл, `Enter` следующая волна; развилка:
`1–3` выбрать тропу (подсказки прямо на карточках).

## v0.8: тултипы

Два паттерна: `.hero-tooltip` (карты руки, hover снизу вверх) и общий
`[data-tip] + .pop` — стилизованный поповер для предметов билда, рекрутов
таверны, слотов формации и строк альтернатив (системные `title` для
контентных подсказок не используются). На крайних элементах разворачивается
внутрь экрана (`.pop.side`).

## v0.4: развилка и лаборатория

- Экран развилки: три крупные карточки-тропы (обычная / элитная с красной
  строкой проклятия / крип-лагерь) со staggered-появлением; проклятие
  элитки раскрыто текстом до выбора.
- Лаборатория колоды в лавке: карточки рекрутов с портретом и ценой найма;
  увольнение/тренировка — в коллекции, вкладка «Колода», в фазе лавки.
- Проклятия в сайдбаре — красные строки, BKB перечёркивает их.
- Плавность: каскадные входные анимации руки/шопа/маршрутов, SFX-синтезатор
  (select/attack/buy/path/win/lose), тумблеры в настройках, уважение
  prefers-reduced-motion.
- Тултип карты — мягкое окно над картой (`.hero-tooltip`, CSS-hover, без
  системного title): способность, эффект и подсказка «ранг/атрибут»; на
  крайних картах разворачивается внутрь экрана.
- Входные анимации играют только при реальном обновлении коллекции
  (новая раздача, реролл, найм/увольнение): флаги `anim*` в UIState,
  main.js ставит их по типу действия; выбор карты ничего не «всплывает».

## Скриншоты

`docs/screenshots/` — паспорт UI: 01 титул, 02 бой (пустой выбор, idle-приговор),
03 выбранная пятёрка, 04 расчёт, 05 шоп, 06 разбор предмета, 07 песочница,
08 босс, 09 Techies (мины+импульс), 10 развилка. Обновлять при любом
изменении UI (часть «definition of done»). Паспорт v0.8 снят на seed
`VISUAL01` (режим формаций).

## v0.8: «decision» edition — что и почему изменилось

- **Рука — в нижнюю полосу во всю ширину** (конвенция жанра: у Balatro рука
  снизу; петля взгляда сжимается до одной вертикали «выбрал → приговор → CTA»).
- **Центр — не набор панелей, а одна сцена.** Арт линии — фон; лозунг
  «Сломай их оборону» и отдельная панель хода удалены; цель, формация,
  формула и кнопки живут в одном блоке с волосяными разделителями.
- **Главный объект центра — формула + приговор**, не большая формация/VS-сцена:
  бой решается в превью, поэтому игра сама отвечает «что будет при нажатии».
- **Приговор трёх состояний + глиф.** Порог «ЕЩЁ 1 УДАР» строго вычисляемый:
  остаток HP ≤ лучший одиночный удар из руки (считается тем же Sim, что и
  превью). Учит игрока темпу: не жечь коммит на добивание.
- **Правая колонка зависит от фазы** и не показывает каталог целиком:
  активное + релевантные альтернативы против этой башни; в лавке — следующая
  цель (устранён дубль «следующая цель» в подвале лавки и протухшая
  «текущая цель» в сайдбаре).
- **Билд контекстен:** иконки с тултипами в бою, полные карточки с продажей
  в лавке.
- **Run-bar слит в топбар** (seed/автосейв — «настроил и забыл»), совет дня
  свёрнут в кнопку 💡 в журнале, журнал сворачиваем.
- Критерий успеха: игрок не сопоставляет HP башни и урон в разных углах —
  ответ лежит непосредственно перед кнопкой «В бой».

## Арт и IP

Портреты/иконки — публичный CDN Valve, фон линии — сгенерированный
`images/battlefield.jpg` (копируется в `dist/images/` сборкой). Замена на
оригинальные ассеты — только `src/ui/art.js`, UI не меняется.
